[build]
  base    = "apps/dashboard"
  command = "npm install --workspaces=false && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS    = "--workspaces=false"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

